package com.Madina;

public class TaxCalculator2019 implements TaxCalculator {
    //if we don't put methods declared in our interface we will get an error
    //to fix it press Alt+enter -> implement methods
    @Override
    public double calculateTax() {
        return 0;
    }
}
